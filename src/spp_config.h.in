#ifndef SPP_CONFIG_H
#define SPP_CONFIG_H

/* 
 * SPP-UCP Network Configuration
 * This file is auto-generated by CMake from spp_config.h.in
 * Do not modify this file directly - modify CMakeLists.txt instead
 */

/* Transmission (packet_request) Configuration */
#define SPP_TX_IP_ADDRESS "@SPP_TX_IP_ADDRESS@"
#define SPP_TX_PORT @SPP_TX_PORT@

/* Reception (packet_indication) Configuration */
#define SPP_RX_IP_ADDRESS "@SPP_RX_IP_ADDRESS@"
#define SPP_RX_PORT @SPP_RX_PORT@

/* Configuration Metadata */
#define SPP_CONFIG_VERSION "@SPP_CONFIG_VERSION@"
#define SPP_BUILD_TIMESTAMP "@SPP_BUILD_TIMESTAMP@"

/* Validation Macros */
#define SPP_PORT_MIN 1024
#define SPP_PORT_MAX 65535

/* Compile-time validation */
#if SPP_TX_PORT < SPP_PORT_MIN || SPP_TX_PORT > SPP_PORT_MAX
#error "SPP_TX_PORT must be between 1024 and 65535"
#endif

#if SPP_RX_PORT < SPP_PORT_MIN || SPP_RX_PORT > SPP_PORT_MAX
#error "SPP_RX_PORT must be between 1024 and 65535"
#endif

#endif /* SPP_CONFIG_H */